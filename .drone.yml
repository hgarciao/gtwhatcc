pipeline:
  check-versions:
    image: jimador/docker-jdk-8-maven-node
    commands:
      - node -v
      - npm -v
      - bower -v
      - gulp -v
      - java -version
      - chmod +x mvnw
      - chmod +x deploy.sh
