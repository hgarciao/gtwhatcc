pipeline:
  build-node:
    image: sickp/alpine-node:6.11.3-r1
    commands:
     - npm install -g bower gulp-cli
  build-java:
    image: openjdk:8-jre-alpine
    commands:
     - ./mvnw clean
     - ./mvnw package -Pprod -DskipTests
